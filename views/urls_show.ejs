<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<header>
<% include ./partials/_header %>
</header>
    <!-- Add your EJS code here -->
   <h1>ShortURL: <%= shortURL %></h1>
 <form action="/urls/<%= shortURL%>?_method=PUT" method="POST" >
      <input id="editedURL" type="text" name="editedURL" value=<%= longURL %> style="width:400px;height:25px;font-size:20px;">
      <input type="submit" value="Update" style="width:100px;height:25px;font-size:100px;">
 </form>
 <ul>
  <% urlDatabase[shortURL].visiterLog.forEach(item=>{ %>
    <li> 
    <p> <strong>Vister Id  : <%= item.id %></strong><br>Visited at : <%= item.visitedOn %> </p> 
    </li>
  <% })%>

 </ul>

</body>
</html>